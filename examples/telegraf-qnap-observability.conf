# Telegraf drop-in: Telegraf self-observability and file cardinality hints.
# Install as: /etc/telegraf/telegraf.d/45-qnap-observability.conf

[[inputs.internal]]
  collect_memstats = true
  collect_gostats = false
  per_instance = true
